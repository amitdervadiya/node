<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Book</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen flex items-center justify-center">
    <form action="/updatedata" method="post" class="bg-white shadow-md rounded-lg p-8 w-full max-w-md">
        <input type="text" name="id" id="id" value="<%= singledata.id %>" hidden>

        <div class="mb-4">
            <label for="name" class="block text-gray-700 font-medium mb-2">Name</label>
            <input type="text" id="name" name="name" placeholder="Name" value="<%= singledata.name %>" required
                class="border border-gray-300 rounded-md p-2 w-full focus:ring-2 focus:ring-indigo-400">
        </div>

        <div class="mb-4">
            <label for="category" class="block text-gray-700 font-medium mb-2">Category</label>
            <input type="text" id="category" name="category" placeholder="Category" value="<%= singledata.category %>"
                required class="border border-gray-300 rounded-md p-2 w-full focus:ring-2 focus:ring-indigo-400">
        </div>

        <div class="mb-4">
            <label for="publishdate" class="block text-gray-700 font-medium mb-2">Publish Date</label>
            <input type="date" id="publishdate" name="publishdate" value="<%= singledata.publishdate %>" required
                class="border border-gray-300 rounded-md p-2 w-full focus:ring-2 focus:ring-indigo-400">
        </div>

        <div class="mb-4">
            <label for="author" class="block text-gray-700 font-medium mb-2">Author</label>
            <input type="text" id="author" name="author" placeholder="Author" value="<%=singledata.author %>" required
                class="border border-gray-300 rounded-md p-2 w-full focus:ring-2 focus:ring-indigo-400">
        </div>

        <div class="mb-4">
            <label for="image" class="block text-gray-700 font-medium mb-2">Image URL</label>
            <input type="text" id="image" name="image" placeholder="Image URL" value="<%=singledata.image %>" required
                class="border border-gray-300 rounded-md p-2 w-full focus:ring-2 focus:ring-indigo-400">
        </div>

        <button type="submit"
            class="bg-indigo-600 text-white font-semibold py-2 px-4 w-full rounded-md hover:bg-indigo-700 transition">Update
            Data</button>
    </form>
</body>

</html>